# /slop-scan

Scan for generic, over-smoothed, inflated, or AI-like prose:

`$ARGUMENTS`

Read these files if available:

- `references/writing-skill-common.md`
- `skills/slop-scan/SKILL.md`

Flag specific passages, explain the failure mode, and propose repairs that
preserve meaning, authorial texture, useful compression, mechanism, and
provenance.

